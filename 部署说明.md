# 云迹智慧旅游推荐系统部署说明

## 1. 部署哪个文件？

请上传 `travel-spark-dist.zip`。

解压后会得到 `dist` 里的静态文件：

- `index.html`
- `assets/`

把这些文件放到网站根目录即可。

## 2. 适合部署的平台

可以部署到：

- 宝塔面板静态网站
- Nginx
- Apache
- 腾讯云轻量服务器
- 阿里云服务器
- Vercel
- Netlify
- GitHub Pages

## 3. Nginx 推荐配置

因为本项目是 Vue 单页应用，刷新页面时需要回退到 `index.html`。

```nginx
location / {
  try_files $uri $uri/ /index.html;
}
```

## 4. 关于 API 功能

项目本地开发时使用了 Vite proxy 转发第三方 API，例如：

- `/api/weather`
- `/api/dog`
- `/api/exchange`
- `/api/air`
- `/api/wiki`
- `/api/sun`
- `/api/countriesnow`

如果只部署纯静态文件，页面主体功能可以正常打开，但这些 `/api/...` 实时接口需要服务器配置反向代理，或者改成真实后端接口。

如果只是课程答辩演示，可以优先使用本地运行方式：

```bash
pnpm install
pnpm dev
```

如果要公网真实部署，建议使用 Nginx 增加 `/api` 反向代理，或写一个 Node/Java 后端统一转发 API。

## 5. 默认账号

- 管理员：admin / 123456
- 游客：traveler / 123456

## 6. 打包命令

```bash
pnpm build
```

打包结果在：

```text
dist/
```

